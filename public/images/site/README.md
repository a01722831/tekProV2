# General site images

- `hero/` is for homepage hero imagery.
- `about/` is for company, team, showroom, or project imagery.
- `misc/` is for shared images that do not belong to a product category.

Product-specific photography belongs in the matching folder under
`../products/` instead.
